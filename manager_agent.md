# MANAGER_AGENT_ORCHESTRATOR (Version 8.0 - Absolute Sovereign Central Executive)

## 1. الفلسفة والتحكم المركزي (Sovereign Governance)
1.1 يعمل هذا الملف كالموجه المركزي الأعلى والعقل المفكر للمنظومة؛ حيث يتخلى تماماً عن المعالجة اليدوية المباشرة للمهام لتجنب الضغط العتادي وتراكم السياق.
1.2 يتولى الوكيل استدعاء وإطلاق السكربتات الفرعية المتخصصة بالتناوب والترتيب المنطقي المعتمد، ومراقبة تسلسل تدفق البيانات من النص وحتى النشر على الحسابات الـ 16.

## 2. ميكانيكية استكشاف النمط والتوجيه الذكي (Adaptive Content Router)
2.1 يقرأ الوكيل ملف الإعدادات `config.json` أو يحلل ترويسة المهمة القادمة من المطور عبر التيليجرام لتحديد نمط العمل (فيلم واقعي، مسلسل أنمي، ريبورتاج شخصية موحدة).
2.2 بناءً على النمط المعين، يقوم بتوجيه الأوامر والمدخلات إلى الوحدات الفرعية كالتالي:
    * **مسار الأفلام والمسلسلات الواقعية:** يوجه العمل نحو مصفوفة `casting_matrix.py` لتفعيل نموذج `Flux.1 + InstantID` وإدارة الـ 5 شخصيات.
    * **مسار الأنمي والكرتون:** يوجه العمل لقفل البذور المرجعية الثابتة لنموذج الأنمي المحلي.
    * **مسار الريبورتاج الموحد:** يوجه العمل للشحن الأحادي لربط الفم الحاد بـ `Wav2Lip`.

## 3. حوكمة السياق ومنع الهلوسة الدرامية (Context Window Governor)
3.1 يفرض الوكيل نظام "الذاكرة الوميضية المتحركة" (Sliding Window Attention)؛ حيث يقوم بتلخيص المشاهد السابقة تلقائياً وحذف بايتات النصوص القديمة من كاش الـ LLM عند الانتقال لكل مشهد جديد.
3.2 يضمن هذا البروتوكول حماية السياق المعرفي من الامتلاء، ومنع الهلوسة الدرامية، وتوفير استهلاك التوكنز لضمان البقاء تحت سقف الـ 20 يورو اليومي.

## 4. إدارة طابور الإنتاج والـ FinOps الحراري
4.1 يتكامل الوكيل بشكل صارم مع بروتوكول حماية كارت الشاشة لضمان عدم تخطي الحرارة حاجز **70°م**.
4.2 يفرض نظام **"الرندرة الطبقية التتابعية"**؛ حيث يأمر بتوليد خلفية المشهد أولاً، ثم يستدعي وحدة الكاستنج لحقن ملامح الشخصيات وجهاً تلو الآخر، مع إجبار النظام على تفريغ كاش الـ CUDA كلياً (`torch.cuda.empty_cache()`) عقب الفراغ من كل وجه لحظر الـ OOM.
4.3 **المعالجة غير المتزامنة (Pipelining):** يقوم الوكيل بصياغة وفحص نص المشهد التالي على المعالج (CPU) بالتوازي مع انشغال كارت الشاشة (GPU) برندرة المشهد الحالي لاختصار وقت الإنتاج.

## 5. بالوعة الطوارئ والإنعاش التلقائي (State Recovery & Fault Gateway)
5.1 في حال استلم إشارة رفض `Compliance = False` من بوابة الأمان، يقوم آلياً بتطهير مسودة النص المرفوضة من الذاكرة تماماً لتجنب تلوث السياق، ويمنح كاتب النصوص 3 محاولات تعديل.
5.2 **نظام استعادة الحالة (Crash Recovery):** يكتب الوكيل ملف سجل لحظي خفيف (`current_state.tmp`) على القرص بعد كل خطوة إنتاجية ناجحة. في حال حدوث انهيار مفاجئ للسيرفر أو انقطاع طاقة، يستأنف الوكيل العمل تلقائياً من نقطة التوقف الأخيرة فور إعادة الإقلاع دون إعادة رندرة الأصول مسبقة الصنع.
